#include <iostream>
#include <algorithm>
#include <functional>
#include "MatrixG.hpp"

using namespace std;

int main(int argc, char* argv[]){
    // MatrixG m = MatrixG(100,100);
    // cout << m.get(0, 0) << endl;
    // int s = 10;
    // for (int k = 0; k < s; k++) {
    //     m.forEach([&](int i, int j, short v, int alive) -> short {
    //         cout << alive << endl;
    //         return v;
    //     });
    // }
    // m.print();
}
